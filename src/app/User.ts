export interface User {
    id: string
    name: string
    email: string
    role:  string
    phone: string
    password: string
    accessrole: string
    isActive: boolean
    logged: boolean
    imageUrl: string
}